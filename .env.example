# Agio Docs Environment Configuration
# Copy this file to .env and fill in the values
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your credentials
#   yarn dev
#

# =============================================================================
# Auth0 Authentication (Required for protected docs)
# =============================================================================
# Get these from your Auth0 dashboard: https://manage.auth0.com/
# Make sure to add http://localhost:5173 to Allowed Callback URLs and Logout URLs

VITE_AUTH0_DOMAIN=
VITE_AUTH0_CLIENT_ID=

# =============================================================================
# SpectaQL API Documentation Generation (Optional)
# =============================================================================
# These credentials are used at build time to introspect GraphQL schemas

# API key for Platform API (api-develop.agiodigital.com)
AGIO_API_KEY=

# Admin secret for Hasura (develop-agiodigital.hasura.app)
HASURA_ADMIN_SECRET=
